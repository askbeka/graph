import React from 'react';
import s from './NotFound.less';

export default (props) => (
    <div className={s.root}>
        <div className={s.container}>
            <h3>Sorry, the page you were trying to view does not exist.</h3>
        </div>
    </div>
);


